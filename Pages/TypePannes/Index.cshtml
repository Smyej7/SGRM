@page
@model SGRM.Pages.TypePannes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypePannes[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TypePannes) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TypePanneId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TypePanneId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TypePanneId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>